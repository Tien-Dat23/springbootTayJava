server:
  port: 8081
spring:
  config:
    activate:
      on-profile: test
openapi:
  service:
    api-docs: api-service
    server: http://test.api-service.local
    title: API Service
    version: 1.0.0
#
